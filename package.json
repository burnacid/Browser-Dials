{
  "name": "browser-dials",
  "private": true,
  "version": "1.0.0",
  "description": "Root workspace — icon generation only",
  "scripts": {
    "icons": "node generate-icons.js"
  },
  "dependencies": {
    "canvas": "^2.11.2"
  }
}
